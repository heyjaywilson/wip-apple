name: app-core-tests
run-name: AppCore tests by {{github.actor}}
on: 
	push
	workflow_dispatch

jobs:
  build:

	  runs-on: macos-latest
	  
	  steps:
		  - uses: actions/checkout@v3
		  - name: Build
		    run: swift build